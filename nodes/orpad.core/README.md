# OrPAD Core Nodes

Built-in control node declarations for OrPAD graph and tree structure.

This pack is shipped with OrPAD, loaded before user-installed packs, and owns the reserved `orpad.*` core node namespace.
